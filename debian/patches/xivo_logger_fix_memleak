Index: asterisk-13.9.1/main/logger.c
===================================================================
--- asterisk-13.9.1.orig/main/logger.c
+++ asterisk-13.9.1/main/logger.c
@@ -168,6 +168,7 @@ static void logmsg_free(struct logmsg *m
 	if (msg->callid) {
 		ast_callid_unref(msg->callid);
 	}
+	ast_string_field_free_memory(msg);
 	ast_free(msg);
 }
 
